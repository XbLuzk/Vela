{
  "name": "coding-smoke",
  "cases": [
    {
      "id": "fix-zero-division",
      "prompt": "修复 calculator.py：除数为 0 时返回 None，并确保现有 unittest 通过。直接修改文件并运行测试。",
      "files": {
        "calculator.py": "def divide(left, right):\n    return left / right\n",
        "test_calculator.py": "import unittest\n\nfrom calculator import divide\n\n\nclass DivideTest(unittest.TestCase):\n    def test_zero_returns_none(self):\n        self.assertIsNone(divide(10, 0))\n\n    def test_normal_division(self):\n        self.assertEqual(divide(10, 2), 5)\n\n\nif __name__ == '__main__':\n    unittest.main()\n"
      },
      "assertions": [
        {
          "type": "command_succeeds",
          "command": ["python", "-m", "unittest", "-q"]
        }
      ]
    },
    {
      "id": "implement-slugify",
      "prompt": "实现 text_utils.py 中的 slugify：去掉首尾空白、转小写，并把连续空白替换成单个短横线。运行 unittest 验证。",
      "files": {
        "text_utils.py": "def slugify(value):\n    raise NotImplementedError\n",
        "test_text_utils.py": "import unittest\n\nfrom text_utils import slugify\n\n\nclass SlugifyTest(unittest.TestCase):\n    def test_normalizes_text(self):\n        self.assertEqual(slugify('  Hello   Vela  '), 'hello-vela')\n\n\nif __name__ == '__main__':\n    unittest.main()\n"
      },
      "assertions": [
        {
          "type": "command_succeeds",
          "command": ["python", "-m", "unittest", "-q"]
        }
      ]
    },
    {
      "id": "preserve-api-contract",
      "prompt": "修改 users.py，让 find_user 找不到用户时返回 None；不要改变已有函数签名，并运行 unittest。",
      "files": {
        "users.py": "USERS = {'u1': {'name': 'Ada'}}\n\ndef find_user(user_id):\n    return USERS[user_id]\n",
        "test_users.py": "import unittest\n\nfrom users import find_user\n\n\nclass FindUserTest(unittest.TestCase):\n    def test_existing_user(self):\n        self.assertEqual(find_user('u1')['name'], 'Ada')\n\n    def test_missing_user(self):\n        self.assertIsNone(find_user('missing'))\n\n\nif __name__ == '__main__':\n    unittest.main()\n"
      },
      "assertions": [
        {
          "type": "command_succeeds",
          "command": ["python", "-m", "unittest", "-q"]
        }
      ]
    }
  ]
}
