[project]
name = "vela-agent"
version = "0.6.0"
description = "Vela local Web AI agent with an explicit ReAct loop, LangGraph planning, MCP, memory, and multimodal input"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Vela Contributors" }]
keywords = ["agent", "ai", "llm", "mcp", "react", "web"]
dependencies = [
  "aiosqlite>=0.22,<1",
  "fastapi>=0.116,<1",
  "filelock>=3.16,<4",
  "httpx>=0.27.0",
  "langgraph>=1.2,<2",
  "langgraph-checkpoint-sqlite>=3.1,<4",
  "mcp>=1.27,<2",
  "pillow>=10.0",
  "pydantic>=2,<3",
  "uvicorn>=0.35,<1",
]

[project.scripts]
vela = "vela.entrypoints.web:app"

[project.optional-dependencies]
dev = [
  "pytest>=8.2.0",
  "ruff>=0.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
# Security floor for a transitive dependency: cryptography < 50.0.0 is affected by
# PYSEC-2026-3552.
constraint-dependencies = ["cryptography>=50.0.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/vela", "src/vela_rag"]

[tool.hatch.build.targets.sdist]
include = [
  "/CONTRIBUTING.md",
  "/LICENSE",
  "/README.md",
  "/docs",
  "/pyproject.toml",
  "/src",
  "/tests",
  "/uv.lock",
  "/web",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = [
  "live: calls real external model, MCP, or browser dependencies",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"
